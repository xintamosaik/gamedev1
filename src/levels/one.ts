
import { tree, bush, shrine } from 'objects/objects';
import type { Level } from 'levels/types';
const levelOne: Level = {
    background: { color: '#2c3e50' },
    statics: [ tree, bush, shrine ],
};

export default levelOne;
