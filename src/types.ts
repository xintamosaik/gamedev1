export type ID = number;

export type Position = {
    x: number;
    y: number;
}

export type Dimensions = {
    w: number;
    h: number; 
}
export type Velocity = {
    vx: number;
    vy: number;
}

export type Render = {
    color: string;
}