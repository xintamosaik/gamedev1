export type Position = {
    x: number;
    y: number;
}

export type Velocity = {
    vx: number;
    vy: number;
}

export type Render = {
    color: string;
}
export type ID = number;

export type Thing = {
    id: ID,
};

